{
  "name": "@canonbridge/lead-capture-edge",
  "version": "1.0.0",
  "description": "Lead capture edge worker with Turnstile verification and rate limiting",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest --run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240512.0",
    "typescript": "^6.0.3",
    "vitest": "^1.6.0",
    "wrangler": "^3.57.0"
  }
}
