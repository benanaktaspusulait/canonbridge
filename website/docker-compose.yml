services:
  website:
    build:
      context: .
      args:
        NEXT_PUBLIC_SITE_URL: http://localhost:8090
        NEXT_PUBLIC_LEAD_CAPTURE_ENABLED: "false"
    ports:
      - "8090:3000"
    restart: unless-stopped
