# ============================================================================
# TRANSFORMER SECRET EXAMPLE - DO NOT COMMIT REAL VALUES
# ============================================================================
# Create the secret manually:
#
# kubectl create secret generic transformer-secret \
#   --from-literal=API_KEY=your-secure-api-key \
#   --from-literal=KAFKA_SASL_USERNAME=your-kafka-user \
#   --from-literal=KAFKA_SASL_PASSWORD=your-kafka-password \
#   -n canonbridge
# ============================================================================

apiVersion: v1
kind: Secret
metadata:
  name: transformer-secret
  namespace: canonbridge
  labels:
    app: transformer
    app.kubernetes.io/name: transformer
    app.kubernetes.io/part-of: canonbridge
type: Opaque
stringData:
  API_KEY: ""
  KAFKA_SASL_USERNAME: ""
  KAFKA_SASL_PASSWORD: ""
