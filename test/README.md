# CanonBridge Tests

This directory is reserved for implementation tests.

Current executable planning-level tests live in:

- `scripts/validate-mapping-fixtures.mjs`
- `scripts/check-schema-compatibility.mjs`

They validate the sample partner mapping package under `partners/` against schemas under `schemas/`.
